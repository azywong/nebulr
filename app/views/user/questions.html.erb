<% if @questions %>
  <ul>
    <% @questions.each do |question| %>
      <li><%= link_to question.text, question_path(question.id) %></li>
    <% end %>
  </ul>
<% else %>
  <p>you have no questions!</p>
<% end %>